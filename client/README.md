# Campus Marketplace Client

This folder contains the React frontend for the Campus Marketplace app.

## Setup

1. Open a terminal in the `client` folder.
2. Run `npm install`.
3. Run `npm run dev` to start the frontend.

## Backend integration

The app proxies `/api` and `/search` requests to `http://localhost:5000`, so start the server from the `server` folder first.
